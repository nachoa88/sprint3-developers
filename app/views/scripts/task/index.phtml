<?php

foreach($this->message as $task){
    echo "<div>";
    echo "<div>" . $task->id . "</div>";
    echo "<div>" . $task->name . "</div>";
    echo "<div>" . $task->status . "</div>";
    echo "<div>" . $task->dateTimeStarted . "</div>";
    echo "<div>" . $task->dateTimeFinished . "</div>";
    echo "<div>" . $task->user . "</div>";
    echo "</div>";
}
